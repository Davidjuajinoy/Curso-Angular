import { HeroeSuperheroPipe } from './heroe-superhero.pipe';

describe('HeroeSuperheroPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroeSuperheroPipe();
    expect(pipe).toBeTruthy();
  });
});
